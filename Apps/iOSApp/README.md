# iOSApp (placeholder)

iOS target is **not built in MVP**. macOS app ships first.

When iOS is on the roadmap, this directory will mirror the structure of
`Apps/MacApp/` and reuse the same Swift packages from `Packages/`.

See [§4 of the macOS design doc](https://github.com/hlexx/EMAIL_ALF/blob/main/14_macos_app_design.md)
for the shared package layout.
