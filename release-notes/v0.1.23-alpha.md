# Re:Box 0.1.23-alpha

- Removed concrete example bodies from runtime AI prompts after live testing showed the local model could copy them into drafts.
- Kept examples in PromptTask metadata for registry coverage and evaluations, but stopped feeding copyable examples to generation.
- Preserved task-specific JSON seeding and placeholder/schema rejection from the previous fixes.
