# Re:Box 0.1.20-alpha

- Further hardened local AI parsing for draft replies and briefs when the model writes plain text after the forced JSON opening brace.
- Added salvage for partial JSON outputs that already contain complete reply or brief fields.
- Kept invalid structured values, such as out-of-range confidence, rejected instead of silently accepting them.
