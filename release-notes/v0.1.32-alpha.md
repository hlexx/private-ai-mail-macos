# PrivateAIMail v0.1.32-alpha

- Extracted MainScene layout, action, compose, and translation wiring into app-local helpers while preserving existing behavior.
- Added focused Mac app tests for layout policy, action routing, and translation policy.
- Kept Trust MVP routing guarded through the supervised action path and validated the default Trust MVP gate.
