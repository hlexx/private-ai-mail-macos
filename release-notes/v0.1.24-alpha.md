# Re:Box 0.1.24-alpha

- Added versioned cache identity for thread briefs so old malformed cached briefs are regenerated after prompt/parser changes.
- Stored the current thread brief prompt and schema versions with new brief rows.
- Updated background brief generation counts and backfill to consider only current-version brief artifacts.
