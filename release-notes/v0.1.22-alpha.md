# Re:Box 0.1.22-alpha

- Removed placeholder JSON examples from local model task prompts after live testing showed the model could copy `...` into the draft.
- Added parser guards that reject placeholder content such as `...` in drafts, briefs and attachment summaries.
- Kept task-specific JSON response seeding so draft replies start from the `body` field and briefs start from the `summary` field.
