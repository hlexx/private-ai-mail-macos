# Re:Box 0.1.21-alpha

- Fixed local AI drafts and briefs accepting JSON Schema echo such as `type` as generated content.
- Changed structured generation to seed each task with its expected first JSON key, for example `body` for draft replies.
- Kept the typed PromptTask schemas in the registry while using compact output shapes in prompts for small local models.
