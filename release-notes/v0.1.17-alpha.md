# Re:Box 0.1.17-alpha

- Fixed draft reply generation appearing as an empty composer when the local model returned a reply with relaxed field names or plain text instead of strict JSON.
- Added an explicit draft-generation failure state with a Retry action.
- Added privacy-safe draft generation logs with error categories only; email bodies and model output are not logged.
