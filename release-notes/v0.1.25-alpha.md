# Re:Box 0.1.25-alpha

- Prevented placeholder draft bodies like `...`, `type`, and schema labels from appearing in the inline composer.
- Invalid cached draft replies are now discarded and regenerated instead of being shown as sendable text.
- The inline composer now disables Send and Edit in full unless the draft body is meaningful.
